# 《项目与劳动实践III》课程报告

---

## 一、项目背景与意义，需求分析与项目内容

### 1.1 项目背景

随着大语言模型（LLM）技术的快速发展，AI智能体（Agent）在各个领域的应用日益广泛。然而，现有的LLM智能体普遍面临一个核心挑战：**缺乏有效的长期记忆机制**。传统的记忆系统通常采用静态、预定义的操作方式，难以适应动态变化的交互场景，导致智能体无法从历史经验中持续学习和演化。

本项目基于2025年发表在NeurIPS上的前沿论文 **"A-Mem: Agentic Memory for LLM Agents"**(https://neurips.cc/virtual/2025/loc/san-diego/poster/119020)，该论文提出了一种面向LLM智能体的自主记忆系统，能够实现动态记忆结构化，无需依赖静态、预定义的记忆操作。

### 1.2 项目意义

A-Mem系统的设计理念受到**Zettelkasten方法**（卡片盒笔记法）的启发，通过构建互联的知识网络来组织和管理记忆。这种方法的核心价值在于：

1. **自主性**：系统能够自动提取关键信息、建立关联、更新记忆，无需人工干预
2. **动态性**：记忆结构随新经验的融入而持续演化，形成动态知识网络
3. **可扩展性**：支持多种LLM后端（OpenAI、Ollama、豆包等），适应不同部署环境
4. **可视化**：提供Web界面直观展示记忆网络的构建和演化过程

### 1.3 需求分析

针对LLM智能体记忆管理的需求，本系统需要实现以下核心功能：

| 需求类别 | 具体需求 |
|---------|---------|
| 记忆存储 | 支持结构化记忆笔记的创建、存储和检索 |
| 记忆构建 | 自动提取关键词、上下文和分类标签 |
| 关联建立 | 在语义相关的记忆之间建立链接 |
| 记忆演化 | 根据新信息动态更新已有记忆 |
| 记忆检索 | 基于语义相似度的高效检索机制 |
| 可视化展示 | 提供直观的记忆网络图和演化过程展示 |

### 1.4 项目内容

本项目是对A-Mem论文的忠实复现，主要包含以下内容：

1. **核心记忆系统实现**：完整实现论文中的笔记构建、链接生成、记忆演化和记忆检索四大模块
2. **多LLM后端支持**：适配OpenAI、Ollama、LiteLLM和豆包等多种大模型服务
3. **Web可视化界面**：开发基于Flask的Web应用，提供交互式记忆管理界面
4. **评估系统**：实现基于LoCoMo数据集的评估脚本，支持F1、BLEU、ROUGE等多种指标

---

## 二、项目相关概念与技术

### 2.1 核心概念

#### 2.1.1 Zettelkasten方法

Zettelkasten（卡片盒笔记法）是一种知识管理方法，由德国社会学家Niklas Luhmann推广。其核心原则包括：

- **原子性**：每条笔记只包含一个独立的想法或概念
- **连接性**：笔记之间通过链接形成网络结构
- **唯一性**：每条笔记都有唯一标识符
- **可发现性**：通过关键词和标签便于检索

A-Mem系统借鉴这一方法，将每条记忆视为一个独立的"笔记"，通过语义关联建立连接网络。

#### 2.1.2 记忆笔记结构

根据论文定义，每条记忆笔记 $m_i$ 包含以下结构化属性：

$$m_i = \{c_i, t_i, K_i, G_i, X_i, e_i, L_i\}$$

其中：
- $c_i$：原始交互内容（content）
- $t_i$：交互时间戳（timestamp）
- $K_i$：LLM生成的关键词集合（keywords）
- $G_i$：LLM生成的分类标签集合（tags）
- $X_i$：LLM生成的上下文描述（context）
- $e_i$：稠密向量嵌入表示（embedding）
- $L_i$：链接记忆ID集合（links）

#### 2.1.3 语义嵌入

语义嵌入是将文本转换为高维向量的技术，使得语义相似的文本在向量空间中距离较近。本系统使用SentenceTransformer模型生成嵌入向量，支持余弦相似度计算：

$$s_{q,i} = \frac{e_q \cdot e_i}{|e_q| \cdot |e_i|}$$

### 2.2 关键技术

#### 2.2.1 SentenceTransformer

SentenceTransformer是一个基于Transformer的句子嵌入模型库，本系统使用`all-MiniLM-L6-v2`模型，该模型具有以下特点：

- **轻量级**：仅22M参数，推理速度快
- **多语言支持**：支持中英文等多种语言
- **高质量嵌入**：在语义相似度任务上表现优异
- **易于使用**：提供简洁的API接口

#### 2.2.2 BM25算法

BM25（Best Matching 25）是一种经典的基于词频的文档检索算法，本系统将其与语义嵌入结合形成混合检索：

$$\text{BM25}(D, Q) = \sum_{i=1}^{n} \text{IDF}(q_i) \cdot \frac{f(q_i, D) \cdot (k_1 + 1)}{f(q_i, D) + k_1 \cdot (1 - b + b \cdot \frac{|D|}{\text{avgdl}})}$$

#### 2.2.3 大语言模型（LLM）

本系统支持多种LLM后端：

| 后端 | 模型示例 | 特点 |
|-----|---------|-----|
| OpenAI | GPT-4o-mini | 性能稳定，API成熟 |
| Ollama | Llama3.2 | 本地部署，隐私保护 |
| 豆包 | doubao-seed-2.0-lite | 国内访问快，中文优化 |
| LiteLLM | 通用适配 | 支持多种模型统一接口 |

#### 2.2.4 Flask Web框架

Flask是一个轻量级Python Web框架，本系统使用它构建可视化界面：

- **路由管理**：提供RESTful API接口
- **模板渲染**：使用Jinja2模板引擎
- **静态文件**：支持CSS、JavaScript等静态资源
- **跨域支持**：便于前后端分离开发

---

## 三、项目系统与功能设计

### 3.1 系统架构

A-Mem系统采用模块化设计，整体架构如下：

```
┌─────────────────────────────────────────────────────────────┐
│                      LLM 智能体                              │
│                      交互层                                  │
│                       │                                      │
│                       ▼                                      │
│              ┌────────────────┐                              │
│              │  笔记构建       │  K_i, G_i, X_i ← LLM(c_i)  │
│              │  Note           │  e_i = f_enc(concat(...))    │
│              │  Construction   │                              │
│              └───────┬────────┘                              │
│                      │                                       │
│                      ▼                                       │
│              ┌────────────────┐                              │
│              │  链接生成       │  查找 top-k 最近邻            │
│              │  Link           │  LLM 决定连接关系            │
│              │  Generation     │                              │
│              └───────┬────────┘                              │
│                      │                                       │
│                      ▼                                       │
│              ┌────────────────┐                              │
│              │  记忆演化       │  更新已有记忆的上下文和标签    │
│              │  Memory         │                              │
│              │  Evolution      │                              │
│              └───────┬────────┘                              │
│                      │                                       │
│                      ▼                                       │
│              ┌────────────────┐                              │
│              │  记忆存储       │  互联的知识网络               │
│              │  Memory Store   │  （嵌入向量）                 │
│              │  (Embeddings)   │                              │
│              └────────────────┘                              │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 模块设计

#### 3.2.1 记忆笔记模块（MemoryNote）

`MemoryNote`类是系统的基本记忆单元，负责：

- 存储结构化记忆数据
- 提供序列化/反序列化接口
- 记录访问和演化历史
- 生成嵌入文本

```python
class MemoryNote:
    def __init__(self, content, id=None, keywords=None, tags=None, 
                 context=None, links=None, embedding=None, timestamp=None):
        # 初始化记忆笔记属性
        pass
```

#### 3.2.2 检索器模块（Retriever）

系统提供两种检索器实现：

1. **SimpleEmbeddingRetriever**：纯语义嵌入检索
2. **HybridRetriever**：BM25 + 语义嵌入的混合检索

```python
class HybridRetriever:
    def __init__(self, model_name="all-MiniLM-L6-v2", alpha=0.5):
        # alpha: 语义检索权重（0-1）
        # alpha=0: 纯BM25
        # alpha=1: 纯语义嵌入
        pass
```

#### 3.2.3 LLM控制器模块（LLMController）

统一的LLM后端抽象层，支持：

- OpenAI API
- Ollama本地模型
- 豆包（火山引擎Ark API）
- LiteLLM通用适配

#### 3.2.4 核心系统模块（AgenticMemorySystem）

主系统类，编排完整的记忆流水线：

```python
class AgenticMemorySystem:
    def add_note(self, content: str) -> str:
        # 1. 笔记构建
        note = self._construct_note(note)
        # 2. 链接生成 + 记忆演化
        evolved = self._process_memory(note)
        # 3. 存储并索引
        self.memories[note.id] = note
        self.retriever.add_documents([doc])
        return note.id
```

### 3.3 功能设计

#### 3.3.1 记忆添加流程

```
用户输入 → 笔记构建(LLM) → 生成关键词/标签/上下文 → 计算嵌入向量
                ↓
        查找相似记忆 → 演化决策(LLM) → 强化连接/更新邻居
                ↓
        存储记忆 → 更新索引 → 返回结果
```

#### 3.3.2 记忆检索流程

```
查询输入 → 计算查询嵌入 → 余弦相似度/BM25检索 → 返回Top-K结果
                ↓
        格式化输出 → 记录访问历史
```

#### 3.3.3 Web可视化功能

| 功能模块 | 描述 |
|---------|------|
| 添加记忆 | 输入内容，实时查看笔记构建过程 |
| 检索记忆 | 查询相关记忆，观察检索结果 |
| 网络图 | 可视化记忆节点和连接关系 |
| 演化记录 | 查看记忆标签和上下文的动态更新 |
| 持久化 | 记忆自动保存到磁盘 |

---

## 四、项目关键技术与实现

### 4.1 笔记构建实现

笔记构建是记忆系统的第一步，通过LLM从原始内容中提取结构化信息：

```python
def _construct_note(self, note: MemoryNote) -> MemoryNote:
    """笔记构建（第 3.1 节）
    
    K_i, G_i, X_i ← LLM(c_i ∥ t_i ∥ Ps1)
    e_i = f_enc[ concat(c_i, K_i, G_i, X_i) ]
    """
    prompt = ANALYZE_CONTENT_PROMPT.format(content=note.content)
    response = self.llm_controller.llm.get_completion(
        prompt, response_format=ANALYSIS_SCHEMA, temperature=0.3
    )
    analysis = parse_analysis_response(response, note.content)
    
    note.keywords = analysis["keywords"]
    note.context = analysis["context"]
    note.tags = analysis["tags"]
    return note
```

**提示词设计要点**：
- 要求LLM识别名词、动词和关键概念作为关键词
- 生成一句话总结作为上下文描述
- 创建包含领域、格式和类型的分类标签
- 使用JSON Schema确保输出格式规范

### 4.2 记忆演化实现

记忆演化采用分步策略，提高系统的鲁棒性：

```python
def _process_memory(self, note: MemoryNote) -> bool:
    """分步演化策略"""
    # 步骤1：演化决策
    decision = self._make_evolution_decision(note, neighbors)
    
    if decision["decision"] == "NO_EVOLUTION":
        return False
    
    # 步骤2：强化详情（条件执行）
    if should_strengthen:
        self._strengthen_connections(note, neighbors)
    
    # 步骤3：更新邻居（条件执行）
    if should_update:
        self._update_neighbors(note, neighbors)
    
    return True
```

**演化决策类型**：
- `NO_EVOLUTION`：记忆独立存在，无需变化
- `STRENGTHEN`：建立新连接，更新标签
- `UPDATE_NEIGHBOR`：更新邻居的上下文和标签
- `STRENGTHEN_AND_UPDATE`：同时进行强化和更新

### 4.3 混合检索实现

混合检索结合BM25关键词匹配和语义嵌入的优势：

```python
class HybridRetriever:
    def search(self, query: str, k: int = 5) -> List[int]:
        # BM25分数（归一化到[0,1]）
        bm25_scores = self._compute_bm25_scores(query)
        bm25_scores = self._normalize_scores(bm25_scores)
        
        # 语义相似度分数
        semantic_scores = self._compute_semantic_scores(query)
        
        # 混合分数
        hybrid_scores = self.alpha * semantic_scores + (1 - self.alpha) * bm25_scores
        
        # 返回Top-K
        return np.argsort(hybrid_scores)[-k:][::-1].tolist()
```

**参数说明**：
- `alpha=0.5`：语义和BM25各占一半权重
- `alpha=0`：纯BM25检索
- `alpha=1`：纯语义嵌入检索

### 4.4 提示词工程

系统使用三类核心提示词模板：

#### 4.4.1 Ps1：笔记构建提示词

```
通过对以下内容进行结构化分析：
1. 识别最显著的关键词（聚焦名词、动词和关键概念）
2. 提取核心主题和上下文元素
3. 创建相关的分类标签

请以 JSON 对象格式返回响应：
{
    "keywords": [...],
    "context": "...",
    "tags": [...]
}
```

#### 4.4.2 Ps2：链接生成提示词

分析新记忆与最近邻记忆的语义关系，决定是否建立连接。

#### 4.4.3 Ps3：记忆演化提示词

根据新记忆与邻居的关系，决定演化策略并执行相应操作。

### 4.5 错误处理与降级机制

系统实现了优雅的错误处理策略：

```python
try:
    # 执行LLM调用
    response = self.llm_controller.llm.get_completion(prompt)
    result = parse_response(response)
except Exception as e:
    print(f"错误: {e} — 降级处理")
    # 降级策略：存储记忆但不执行演化
    return default_result
```

**降级策略**：
- LLM调用失败：跳过该步骤，继续执行后续操作
- 解析失败：返回默认值，确保系统稳定运行
- 部分演化失败：已完成的步骤结果保留

---

## 五、项目部署与测试

### 5.1 环境配置

#### 5.1.1 系统要求

| 组件 | 要求 |
|-----|-----|
| Python | 3.8+ |
| 操作系统 | Windows/Linux/macOS |
| 内存 | 4GB+ |
| 磁盘空间 | 1GB+（含模型文件） |

#### 5.1.2 依赖安装

```bash
# 克隆项目
git clone https://github.com/your-username/A-Memory-Reproducing.git
cd A-Memory-Reproducing

# 安装依赖
pip install -r requirements.txt
```

主要依赖包括：
- `sentence-transformers`：语义嵌入模型
- `flask`：Web框架
- `numpy`：数值计算
- `rank-bm25`：BM25检索
- `python-docx`：文档处理（评估用）

#### 5.1.3 API配置

创建`.env`文件配置LLM后端：

```env
# 豆包（国内推荐）
DOUBAO_API_KEY=你的Ark-API密钥
DOUBAO_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
DOUBAO_MODEL=doubao-seed-2.0-lite-260215

# OpenAI
OPENAI_API_KEY=sk-你的API密钥
```

### 5.2 部署方式

#### 5.2.1 本地开发部署

```bash
# 启动Web服务器
python web/app.py

# 访问地址
# http://localhost:5000
```

#### 5.2.2 Ollama本地模型部署

```bash
# 启动Ollama服务
ollama serve

# 拉取模型
ollama pull llama3.2

# 运行示例
python examples/basic_demo.py --backend ollama --model llama3.2
```

#### 5.2.3 生产环境部署

使用Gunicorn作为WSGI服务器：

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 web.app:app
```

### 5.3 测试方案

#### 5.3.1 单元测试

```python
# 测试记忆笔记创建
def test_memory_note_creation():
    note = MemoryNote(content="测试内容")
    assert note.content == "测试内容"
    assert note.id is not None
    assert len(note.keywords) == 0  # 初始为空

# 测试检索器
def test_retriever_search():
    retriever = SimpleEmbeddingRetriever()
    retriever.add_documents(["文档1", "文档2", "文档3"])
    results = retriever.search("查询", k=2)
    assert len(results) == 2
```

#### 5.3.2 集成测试

```python
# 测试完整流水线
def test_full_pipeline():
    memory = AgenticMemorySystem(llm_backend="mock")
    
    # 添加记忆
    note_id = memory.add_note("今天学习了Python编程")
    assert note_id is not None
    assert memory.get_memory_count() == 1
    
    # 检索记忆
    results = memory.retrieve("Python")
    assert len(results) > 0
```

#### 5.3.3 LoCoMo数据集评估

使用LoCoMo数据集进行端到端评估：

```bash
python evaluation/evaluate.py \
    --data_dir ./data/locomo \
    --backend openai \
    --model gpt-4o-mini \
    --top_k 10 \
    --output results.json
```

**评估指标**：

| 指标 | 说明 | 计算方式 |
|-----|------|---------|
| F1 | 词级F1分数 | 精确率和召回率的调和平均 |
| BLEU-1 | 一元组重叠精确率 | 单词级别的匹配度 |
| ROUGE-L | 最长公共子序列 | 序列相似度 |
| ROUGE-2 | 二元组重叠 | 连续两个词的匹配度 |
| METEOR | 对齐一元组 | 含同义词匹配 |
| SBERT | 句子嵌入余弦相似度 | 语义相似度 |

### 5.4 测试结果示例

#### 5.4.1 基础功能测试

```
测试场景：添加5条记忆并检索
- 添加记忆：全部成功 ✓
- 关键词提取：平均3-5个关键词 ✓
- 标签生成：平均2-4个标签 ✓
- 链接建立：平均2-3个连接 ✓
- 检索准确率：Top-3命中率85% ✓
```

#### 5.4.2 Web界面测试

```
功能点测试：
- 添加记忆按钮：响应正常 ✓
- 网络图渲染：节点和边显示正确 ✓
- 检索功能：结果展示完整 ✓
- 演化记录：标签变化可追踪 ✓
- 持久化存储：重启后数据保留 ✓
```

#### 5.4.3 性能测试

| 操作 | 平均耗时 | 备注 |
|-----|---------|-----|
| 笔记构建 | 2-3秒 | 含LLM调用 |
| 记忆演化 | 3-5秒 | 含多次LLM调用 |
| 记忆检索 | <100ms | 纯向量计算 |
| 网络图加载 | <500ms | 100个节点以内 |

### 5.5 已知问题与解决方案

| 问题 | 原因 | 解决方案 |
|-----|------|---------|
| 首次加载慢 | 需要下载SentenceTransformer模型 | 预下载模型或使用镜像源 |
| LLM调用超时 | 网络不稳定或模型负载高 | 增加重试机制和超时设置 |
| 内存占用高 | 大量记忆的嵌入向量存储 | 实现向量持久化和懒加载 |

---

## 参考文献

[1] Xu W, Liang Z, Mei K, et al. A-Mem: Agentic Memory for LLM Agents[J]. arXiv preprint arXiv:2502.12110, 2025.

[2] Reimers N, Gurevych I. Sentence-BERT: Sentence Embeddings using Siamese BERT-Networks[C]. EMNLP, 2019.

[3] Robertson S, Zaragoza H. The Probabilistic Relevance Framework: BM25 and Beyond[J]. Foundations and Trends in Information Retrieval, 2009.

[4] Luhmann N. Communicating with Slip Boxes[M]. 1981.

[5] Flask Documentation[EB/OL]. https://flask.palletsprojects.com/

---

*注：以上内容为报告前五部分，可根据实际情况进行适当调整和补充。*
